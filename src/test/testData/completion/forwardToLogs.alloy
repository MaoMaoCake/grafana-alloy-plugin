prometheus.remote_write "rw" {
    endpoint {
        url = "http://mimir/push"
    }
}

loki.write "logs" {
    endpoint {
        url = "http://loki/push"
    }
}

loki.source.file "f" {
    targets    = []
    forward_to = [ <caret> ]
}
