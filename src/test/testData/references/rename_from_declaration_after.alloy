prometheus.remote_write "renamed" {
    endpoint {
        url = "http://mimir/push"
    }
}

prometheus.scrape "s" {
    targets    = []
    forward_to = [ prometheus.remote_write.renamed.receiver ]
}
